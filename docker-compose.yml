services:
  ingest_ffmpeg:
    build: ./edge/ingest_ffmpeg
    command: ["sleep", "infinity"]
  pipeline_worker:
    build:
      context: .
      dockerfile: edge/pipeline_worker/Dockerfile
    command: ["sleep", "infinity"]
    volumes:
      - ./src:/app/src
  hls_packager:
    build: ./edge/hls_packager
    command: ["sleep", "infinity"]
  ui_server:
    build: ./ui
    command: ["sleep", "infinity"]
